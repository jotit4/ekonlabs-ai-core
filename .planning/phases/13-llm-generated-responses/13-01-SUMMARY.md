---
phase: 13-llm-generated-responses
plan: "01"
status: complete
completed_at: 2026-04-05
---

# Summary — Plan 13-01: LLM-Generated Responses (Wave 1)

## What was done
- Restructured `generation_node` in `app/agent/nodes/generation.py` to route scheduling and booking intents through the LLM instead of hardcoded f-string templates
- Added `_build_scheduling_context(state, system_prompt_base) -> str` helper: injects slot display strings as plain-text bullet list when slots available; instructs LLM to deliver no-availability message otherwise
- Added `_build_booking_context(state, system_prompt_base) -> str` helper: handles confirm (event_id present / absent), cancel (event found / not found), and ambiguous slot — each branch builds a system directive with exact slot display text injected verbatim
- Removed all hardcoded f-string return paths for scheduling and booking (BOOKING_CONFIRMED_TEMPLATE, BOOKING_FAILED_NO_SLOTS, BOOKING_CANCELLED, BOOKING_NOT_FOUND kept as constants for reference but no longer returned directly)
- Shadow mode and anti-diagnostic bypasses preserved exactly as hardcoded gates (RESP-06, RESP-07)
- Updated ~20 existing tests to match new LLM-invoked behavior (removed template assertions, added `mock_llm.invoke.assert_called_once()`)
- Added 8 new tests: scheduling slots, scheduling no-slots, booking confirm with display, booking cancel, ambiguous slot, shadow/anti-diagnostic hardcoded stays

## Requirements satisfied
- RESP-01 ✓ — scheduling intent uses LLM, slot displays injected verbatim in system context
- RESP-02 ✓ — booking confirmation response generated by LLM with exact `booked_slot["display"]` in system directive
- RESP-03 ✓ — cancellation response generated by LLM
- RESP-04 ✓ — no-availability response generated by LLM
- RESP-05 ✓ — ambiguous slot response generated by LLM
- RESP-06 ✓ — shadow mode bypass stays hardcoded, no LLM call
- RESP-07 ✓ — anti-diagnostic bypass stays hardcoded, no LLM call

## Tests
- Generation tests: 44/44 pass after updates
- New tests added: test_scheduling_slots_system_context_contains_all_displays, test_scheduling_no_slots_system_context_instructs_actionable_step, test_resp02_booking_confirm_display_in_system_context, test_resp03_booking_cancel_success_calls_llm, test_resp05_ambiguous_slot_context_instructs_number_selection, test_resp06_shadow_mode_hardcoded_no_llm, test_resp07_anti_diagnostic_hardcoded_no_llm

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] test_graph_booking_confirm_bypasses_scheduling_and_rag broken by Phase 13**
- **Found during:** Task 4 (full test suite)
- **Issue:** This graph test expected `mock_llm.invoke.assert_not_called()` and checked the BOOKING_CONFIRMED_TEMPLATE format — both assumptions broken since booking now calls LLM
- **Fix:** Changed to `mock_llm.invoke.assert_called_once()`, removed template assertion, added `isinstance(ai_messages[-1], AIMessage)` check
- **Files modified:** tests/test_agent/test_graph.py

## Commit
- `e4c8e3d`: feat(phase-13): route booking+scheduling intents through LLM (RESP-01/02/03/04/05/06/07)

## Self-Check: PASSED
- app/agent/nodes/generation.py — _build_scheduling_context and _build_booking_context added, booking/scheduling paths use _llm.invoke, hardcoded bypasses intact
- tests/test_agent/test_nodes/test_generation.py — updated, new tests pass
- tests/test_agent/test_graph.py — booking confirm test fixed
